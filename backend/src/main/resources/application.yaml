server:
  servlet:
    context-path: /api
spring:
  config:
    import: "optional:configtree:/run/secrets/"
  application:
    name: MioBook
  frontend:
    url: http://localhost:5173
    authCallback: /auth/google/callback
  datasource:
    url: jdbc:mysql://localhost:3306/miobook?createDatabaseIfNotExist=true
    username: root
    password: ${db_root_password}
  jpa:
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  jwt:
    secret: ${jwt_secret}
    accessTokenExpiration: 300 # 5m
    refreshTokenExpiration: 604800 # 7d
  oauth:
    google:
      clientId: 312476898368-5t065nkj5j3miq5m8omg6osm7ldgcg24.apps.googleusercontent.com
      clientSecret: ${oauth_google_client_secret}